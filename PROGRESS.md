# Progress

- [x] MCP26-D Establish the canonical one-package/one-module design hierarchy and commit the reviewed MCP 2026-07-28 contract; responsibility is design ownership and requirement tracing, non-goals are source implementation, package splitting, speculative coordinators/factories, response caching, and Tasks, invariants are the three internal layers (Client/Server orchestration, protocol core, transport/platform I/O), Application ownership of user input/persistence, and unchanged legacy public entry points, and evidence is a root-to-component `DESIGN.md` index plus one owner for every scored scenario and legacy invariant; commit: `Document MCP 2026 dual-era architecture` `depends:none` `parallel:none`

- [x] MCP26-P Implement and commit the state-free protocol core; responsibility is era-aware wire representation, typed 2026 models/errors, and pure header derivation, non-goals are I/O, connection/retry/session ownership, response stores, network `$ref` resolution, and Tasks, invariants are legacy byte compatibility, mandatory modern request `_meta` and response `resultType`, lossless arbitrary `Value`, and no protocol-core dependency on transports; commit: `Add MCP 2026 protocol core` `depends:MCP26-D` `parallel:none`

- [x] MCP26-S Implement and commit the modern Server/Transport boundary; responsibility is per-exchange HTTP/stdio mechanics, normalized dispatch context, modern server lifecycle, bounded subscriptions, per-request MRTR validation, and server-owned method/header semantics, non-goals are client input gathering, cross-request MRTR continuation, OAuth policy, Tasks, changing public `Transport`, and connection-scoped modern semantics, invariants are independent POSTs, request-scoped state, no modern session/GET/DELETE/Last-Event-ID behavior, and isolation across colliding JSON-RPC IDs; commit: `Add MCP 2026 server transport` `depends:MCP26-P` `parallel:none`

- [ ] MCP26-C Implement and commit modern Client orchestration; responsibility is explicit era negotiation, per-request metadata, transient tool-header resolution, bounded MRTR, subscriptions, and pending-request cleanup, non-goals are transport mechanics, schema logic outside `ToolHeaderResolver`, response caching, Tasks/persistence, and a new coordinator/factory actor, invariants are no concrete-transport downcast, one owner per mutable state, fresh JSON-RPC IDs per attempt, exact opaque state echo, and legacy `connect(transport:)` behavior unchanged `depends:MCP26-S` `parallel:none`
  - [ ] MCP26-C.1 Add an explicit modern/dual-era connection API returning `ConnectionInfo`, HTTP origin-lifetime era pinning, per-request metadata/result decoding, and cache-hint exposure without a store; on one live stdio connection classify a valid discovery result or recognized modern error as modern, any other JSON-RPC error or timeout as the signal to send legacy initialize on that same connection, and a closed or malformed stream as typed failure; prove each branch, unsupported version, disconnect, and existing legacy entry-point behavior with timed focused tests `depends:MCP26-S` `parallel:none`
  - [ ] MCP26-C.2 Route modern calls through bounded transient tools-list resolution (positive configurable default 64), invalid-tool filtering, standard/custom headers, exactly one fresh-ID retry after `-32020`, bounded MRTR through application-provided Roots/Sampling/Elicitation handlers (positive configurable `maxRounds` default 10), and subscription demultiplexing/cancellation; prove page-bound and cursor-cycle failure, multi-input/multi-round flows, missing capability/input, exact opaque state echo without client integrity interpretation, round exhaustion, header refresh, concurrent request isolation, and cleanup with timed focused tests `depends:MCP26-C.1` `parallel:none`
  - [ ] MCP26-C.3 Run Client↔Server in-memory, stdio, and stateless-HTTP tests for discovery/direct request, legacy fallback, MRTR, request-scoped progress/logging, subscriptions, header retry, and colliding IDs; inspect actor/task ownership and verify no connection-scoped modern request/client/capability state, obtain original task-designer approval, confirm branch and commit message, and commit only Client scope plus progress state `depends:MCP26-C.2` `parallel:none`

- [ ] MCP26-Q Implement and commit scored OAuth behavior and external conformance adapters; responsibility is 2026 authorization-client correctness and exercising only public SDK paths, non-goals are authorization-server implementation, non-scored auth extensions, Tasks, conformance-only production branches, and expected-failure suppression, invariants are issuer-bound credentials, bounded retries, endpoint/audience validation before token use, pinned `@modelcontextprotocol/conformance@0.2.0-alpha.10`, and zero skipped scored scenarios `depends:MCP26-C,MCP26-S` `parallel:none`
  - [ ] MCP26-Q.1 Bring existing OAuth types through protected-resource discovery variants, issuer consistency/migration, RFC 9207 `iss` validation and normalization, pre-registration then CIMD then DCR selection, DCR `application_type`, supported/omitted/offline scopes, step-up scope union/retry limit, resource mismatch, and basic/post/none token authentication; prove each of the 25 scored `auth/*` success/failure paths with timed focused tests and no secrets in logs/errors `depends:MCP26-C,MCP26-S` `parallel:none`
  - [ ] MCP26-Q.2 Adapt the client conformance executable to select explicit modern or existing legacy public connection APIs from `MCP_CONFORMANCE_PROTOCOL_VERSION` and exercise all 32 scored client scenarios without package-internal access `depends:MCP26-Q.1` `parallel:conformance_apps`
  - [ ] MCP26-Q.3 Adapt the server conformance executable to route 2026 requests through stateless public Server/Transport APIs and earlier revisions through legacy APIs, covering all 37 scored server fixtures without duplicating protocol validation in the executable `depends:MCP26-Q.1` `parallel:conformance_apps`
  - [ ] MCP26-Q.4 Pin the runner to the frozen referee, use per-process/scenario timeouts and deterministic startup/cleanup, run all 37 server and 32 client scored scenarios exactly once at `2026-07-28` with zero failures/skips/baseline exclusions, report `not_scored` separately, inspect external HTTP/stdio wire evidence, obtain original task-designer approval, confirm branch and commit message, and commit only OAuth/conformance scope plus progress state `depends:MCP26-Q.2,MCP26-Q.3` `parallel:none`

- [ ] MCP26-V Verify the committed dual-era SDK as one composition; responsibility is integration evidence, design conformance, and final progress state, non-goals are unrelated improvements or fixes without a scoped counterexample, invariants are every prior public legacy contract, every scored 2026 requirement, bounded state/lifetimes, one-module dependency direction, no incomplete production branch, and no unrelated worktree changes `depends:MCP26-D,MCP26-P,MCP26-S,MCP26-C,MCP26-Q` `parallel:none`
  - [ ] MCP26-V.1 Run the complete Swift test suite with a timeout and confirm all original 551 tests/40 suites plus new tests pass; build `MCP`, `mcp-everything-client`, and `mcp-everything-server`, and verify legacy 2024-11-05 through 2025-11-25 wire fixtures, existing `connect(transport:)`, and an external raw custom-`Transport` consumer still compile and behave unchanged `depends:MCP26-D,MCP26-P,MCP26-S,MCP26-C,MCP26-Q` `parallel:integration_checks`
  - [ ] MCP26-V.2 Re-run the pinned frozen gate with no scored exclusions and retain machine-readable evidence that all named 37 server and 32 client scenarios pass at `2026-07-28`; report Tasks and every other `not_scored` entry separately without claiming implementation `depends:MCP26-D,MCP26-P,MCP26-S,MCP26-C,MCP26-Q` `parallel:integration_checks`
  - [ ] MCP26-V.3 Audit source and designs for layer-direction violations, concrete transport downcasts, modern connection-scoped request/client/capability state, unbounded dictionaries/loops/schema walks, ExchangeID/JSON-RPC-ID confusion, suppressed errors, response-cache/Tasks scope creep, target-conditional ownership differences, and `FIXME(INCOMPLETE_IMPLEMENTATION)`; inspect actual success/failure/cancellation/shutdown behavior, obtain final original task-designer approval, update all progress boxes, confirm branch and commit message, and make the final scoped local commit `depends:MCP26-V.1,MCP26-V.2` `parallel:none`
