# Progress

- [x] MCP26-D Establish the canonical one-package/one-module design hierarchy and commit the reviewed MCP 2026-07-28 contract; responsibility is design ownership and requirement tracing, non-goals are source implementation, package splitting, speculative coordinators/factories, response caching, and Tasks, invariants are the three internal layers (Client/Server orchestration, protocol core, transport/platform I/O), Application ownership of user input/persistence, and unchanged legacy public entry points, and evidence is a root-to-component `DESIGN.md` index plus one owner for every scored scenario and legacy invariant; commit: `Document MCP 2026 dual-era architecture` `depends:none` `parallel:none`

- [x] MCP26-P Implement and commit the state-free protocol core; responsibility is era-aware wire representation, typed 2026 models/errors, and pure header derivation, non-goals are I/O, connection/retry/session ownership, response stores, network `$ref` resolution, and Tasks, invariants are legacy byte compatibility, mandatory modern request `_meta`, default-complete and explicit input-required response semantics, lossless arbitrary `Value`, and no protocol-core dependency on transports; commit: `Add MCP 2026 protocol core` `depends:MCP26-D` `parallel:none`

- [x] MCP26-S Implement and commit the modern Server/Transport boundary; responsibility is per-exchange HTTP/stdio mechanics, normalized dispatch context, modern server lifecycle, bounded subscriptions, per-request MRTR validation, and server-owned method/header semantics, non-goals are client input gathering, cross-request MRTR continuation, OAuth policy, Tasks, changing public `Transport`, and connection-scoped modern semantics, invariants are independent POSTs, request-scoped state, no modern session/GET/DELETE/Last-Event-ID behavior, and isolation across colliding JSON-RPC IDs; commits: `Add MCP 2026 server transport`, `Reject invalid modern HTTP request IDs` `depends:MCP26-P` `parallel:none`

- [x] MCP26-C Implement and commit modern Client orchestration; responsibility is explicit era negotiation, per-request metadata, transient tool-header resolution, bounded MRTR, subscriptions, and pending-request cleanup, non-goals are transport mechanics, schema logic outside `ToolHeaderResolver`, response caching, Tasks/persistence, and a new coordinator/factory actor, invariants are no concrete-transport downcast, one owner per mutable state, fresh JSON-RPC IDs per attempt, exact opaque state echo, and legacy `connect(transport:)` behavior unchanged; commits: `Add MCP 2026 client orchestration`, `Fix modern HTTP response correlation` `depends:MCP26-S` `parallel:none`

- [x] MCP26-Q Implement and commit scored OAuth behavior and external conformance adapters; responsibility is 2026 authorization-client correctness and exercising only public SDK paths, non-goals are authorization-server implementation, non-scored auth extensions, Tasks, conformance-only production branches, and expected-failure suppression, invariants are issuer-bound credentials, bounded retries, endpoint/audience validation before token use, pinned `@modelcontextprotocol/conformance@0.2.0-alpha.10`, and zero skipped scored scenarios; commits: `Complete MCP 2026 conformance support`, `Run pinned MCP 2026 conformance in CI` `depends:MCP26-C,MCP26-S` `parallel:none`

- [x] MCP26-V Verify the committed dual-era SDK as one composition; responsibility is integration evidence, design conformance, and final progress state, non-goals are unrelated improvements or fixes without a scoped counterexample, invariants are every prior public legacy contract, every scored 2026 requirement, bounded state/lifetimes, one-module dependency direction, no incomplete production branch, and no unrelated worktree changes; commits: `Route modern wire through MessageCodec`, `Fix modern HTTP response correlation`, `Run pinned MCP 2026 conformance in CI` `depends:MCP26-D,MCP26-P,MCP26-S,MCP26-C,MCP26-Q` `parallel:none`
